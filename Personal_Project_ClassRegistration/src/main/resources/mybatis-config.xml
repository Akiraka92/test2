<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>

	<!-- DB접속정보 값을 가지고 있는 파일 설정 -->
	<!-- <properties resource="db.properties" /> -->

	<typeAliases>
		<typeAlias type="global.sesoc.classRegistration.vo.User" alias="user"/>
		
		<typeAlias type="global.sesoc.classRegistration.vo.Lecture" alias="lecture"/>
		<typeAlias type="global.sesoc.classRegistration.vo.Major" alias="major"/>
		<typeAlias type="global.sesoc.classRegistration.vo.Regist" alias="regist"/>
		<typeAlias type="global.sesoc.classRegistration.vo.Syllabus" alias="syllabus"/>
		<typeAlias type="global.sesoc.classRegistration.vo.Application" alias="application"/>
		<!-- 
		 -->
		
		
	</typeAliases>



	<!-- DB접속 정보 셋팅
	<environments default="development">
		<environment id="development">
			<transactionManager type="JDBC" />
			<dataSource type="POOLED">
				<property name="driver" value="${driver}" />
				<property name="url" value="${url}" />
				<property name="username" value="${username}" />
				<property name="password" value="${password}" />
			</dataSource>
		</environment>
	</environments>
 -->
 
	<mappers>
		<mapper resource="userMapper.xml" />
		<mapper resource="majorMapper.xml" />
		<mapper resource="lectureMapper.xml" />

	</mappers>

</configuration>
	